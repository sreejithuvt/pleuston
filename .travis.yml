language: node_js
node_js: node

# will run `npm install` automatically

script:
  - npm run build-css
  - npm run test
  - npm run build

notifications:
    email: false

cache:
  directories:
    - node_modules
